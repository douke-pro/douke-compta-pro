// Fichier : routes/company.js
const express = require('express');
const router = express.Router();

// Route de test ou placeholder
// router.get('/', (req, res) => res.send('Company routes working'));

module.exports = router;
