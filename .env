# =========================================================================
# 1. PARAMÈTRES D'ENVIRONNEMENT CRITIQUES (Sécurité)
# =========================================================================

# CLÉ SECRÈTE POUR LE CRYPTAGE DES JWT (Tokens d'authentification).
# CRITIQUE: Utilisez une chaîne longue et aléatoire. NE PAS UTILISER CE TEXTE EN PRODUCTION.
JWT_SECRET="votre_cle_secrete_hyper_securisee_et_aleatoire_pour_le_dev_42"

# SEL POUR LE HASHAGE DES MOTS DE PASSE (Bcrypt).
# 10 est le coût standard et sûr en 2024.
BCRYPT_SALT_ROUNDS=10

# =========================================================================
# 2. CONFIGURATION DE LA BASE DE DONNÉES (PRISMA)
# =========================================================================

# RAPPEL : Votre application utilise l'URL de BDD fournie par Render en production.
# En développement local, remplacez les valeurs par celles de votre base locale (ex: Docker, PostgreSQL local).
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Si vous utilisez un container Docker PostgreSQL local sans mot de passe :
DATABASE_URL="postgresql://postgres:votre_mot_de_passe_local@localhost:5432/douke_compta_db?schema=public"


# =========================================================================
# 3. VARIABLES DE L'APPLICATION
# =========================================================================

# Port d'écoute du serveur Node.js/Express en local
PORT=3000

# URL de la base de données de production sur Render. 
# Cette variable DOIT être configurée directement dans l'interface de Render.
# RENDER_DATABASE_URL="votre_url_de_connexion_render_postgresql"

# URL du frontend (utilisée pour les CORS ou les redirects)
CLIENT_URL="http://localhost:8080"
